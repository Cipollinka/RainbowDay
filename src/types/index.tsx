export interface CalendarHistory {
  doneTasks: number;
  skippedTasks: number;
  date: string;
}

export interface ColorData {
  color: string;
  meaning: string;
  description: string;
  tasks: string[];
}
